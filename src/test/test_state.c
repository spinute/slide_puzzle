#include "../state.h"
#include "./test.h"

TEST_GROUP(state);

TEST_SETUP(state)
{
}

TEST_TEAR_DOWN(state)
{
}

TEST(state, test_test)
{
}

TEST_GROUP_RUNNER(state)
{
    RUN_TEST_CASE(state, test_test);
}
